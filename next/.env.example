WEBSITE_URL=http://localhost:3000 # Add the correct ENV var for this onto your hosting platform, point it to your production website.
PORT=3000

NEXT_PUBLIC_API_URL=http://localhost:1337
PREVIEW_SECRET=tobemodified 

# Pipedrive API Token for Lead Integration (ONLY for zakelijk/business leads)
# Particuliere leads worden ALLEEN opgeslagen in de database, niet in Pipedrive
# Get token from: Pipedrive → Settings → Personal Preferences → API
PIPEDRIVE_API_TOKEN_ZAKELIJK=your-business-pipedrive-token-here
