{
  "kind": "collectionType",
  "collectionName": "email_templates",
  "info": {
    "singularName": "email-template",
    "pluralName": "email-templates",
    "displayName": "Email Template",
    "description": "Templates for HTML emails"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "subject": {
      "type": "string",
      "required": true
    },
    "htmlContent": {
      "type": "richtext",
      "required": true
    },
    "variables": {
      "type": "json"
    },
    "type": {
      "type": "enumeration",
      "enum": ["welkom", "nieuwe-gebruiker", "factuur", "algemeen", "trial-welcome", "trial-expired", "plan-selected"],
      "required": true
    }
  }
}